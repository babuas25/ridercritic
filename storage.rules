rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // TEMPORARY: Allow all reads and writes for development
    // TODO: Integrate Firebase Auth with NextAuth for proper authentication
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
    
    // Uncomment and use when proper authentication is implemented:
    // Reviews images
    // match /reviews/{reviewId}/{allPaths=**} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
    // 
    // Motorcycle images
    // match /motorcycles/{motorcycleId}/{allPaths=**} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}